# Pinned for reproducible installs.
# Resolved together with pip on Python 3.13 (2026-08-17); also valid for Python 3.9+.
# To refresh: pip install --dry-run --report - <unpinned names>
#
# Note: the top-level `langchain` meta-package was removed - nothing in this
# repo imports it. Only langchain-community and langchain-groq are used.

streamlit==1.61.1
pinecone==9.1.0
langchain-community==0.4.2
langchain-groq==1.1.3
sentence-transformers==5.7.0
pandas==3.0.5
python-dotenv==1.2.3
