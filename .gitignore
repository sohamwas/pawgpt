# --- Secrets: never commit API keys ---
# .env holds the real keys; .env.example is the tracked template.
.env
!.env.example
.streamlit/secrets.toml
*.key
groq_api_key.txt

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# --- Local-only legacy stack (pre-Pinecone Flask + ChromaDB prototype) ---
# Kept on disk for reference; deliberately not part of this repo.
app.py
vector_db.py
templates/
db_chroma/

# --- Large local media not tracked in demo/ ---
pawgpt_recording.mp4

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
